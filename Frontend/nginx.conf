server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Vue Router dùng history mode -> mọi route không khớp file tĩnh
    # đều fallback về index.html để Vue Router tự xử lý.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
